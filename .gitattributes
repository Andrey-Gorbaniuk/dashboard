# По умолчанию обрабатывать текстовые файлы автоматически
* text=auto

# Указать, что эти файлы должны ВСЕГДА использовать LF окончания строк
# Это важно для кросс-платформенной разработки и для скриптов,
# которые будут выполняться в Linux-контейнерах.
*.py text eol=lf
*.sh text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf
.dockerignore text eol=lf
.gitignore text eol=lf
requirements.txt text eol=lf